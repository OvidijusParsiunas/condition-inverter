if (dog && cat && mouse) {
  console.log('success');
}

if dog and cat and mouse:
  print(success)

if dog := true; dog && cat {
  fmt.Println(num, "is negative")
}

for (let i = 0; i >= 40; i += 1) {
  console.log('hello');
}

<div v-if="currentView === NAVBAR_MENU_BUTTONS.HOMEPAGE">
  <homepage/>
</div>